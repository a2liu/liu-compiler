test()

add :: proc(a: int, b: s64) s64 {
    return a + b
}

make :: proc(value: int) s64 {
    return add(value, 12)
}

test :: proc() int {
    a : int = make(14)
    return add(a, make(14))
}

int :: s64
